# v37 庫房閱讀整理、資訊庫分類、刪除保留期

適用於已套用 v36 的版本。將 ZIP 解壓後，依原資料夾位置覆蓋 GitHub 專案檔案，再由現有 Cloudflare Worker 部署流程發布。本包尚未部署到正式網站。

## 本次內容
- 庫房改為精簡單列清單，搜尋、收齊／庫存篩選、20／50／100 筆分頁及統一調整排序。無零件的案件顯示「尚無零件」。
- 點案件開啟大型收領料視窗，零件資料預設閱讀模式；有權限者可切換編輯。收料與領料分色，固定表頭與名稱欄，搜尋及篩選保留輸入，提示隱藏的修改項目。關閉後保留本頁草稿及外層篩選／頁碼。
- 資訊庫依案件、庫房、電鍍、線材、系統與權限分類；不確定的舊資料列為未分類。新增日期／關鍵字查詢、20 筆分頁，主管可匯出全部或全部符合篩選的紀錄，匯出視窗列出筆數。
- 庫房、電鍍、線材已刪除清單保留 7 天。新刪除資料由刪除時間起算；更新前沒有到期欄位的資料，由排程首次處理起算 7 天。清單列出到期時間。
- 到期後永久清除該項資料、線捆／裁線紀錄及所屬照片；資訊庫操作紀錄不隨之刪除。7 天內還原的資料不會被排程清除。照片刪除失敗會留在伺服器工作清單待重試。

## 排程部署
必須一併上傳 wrangler.jsonc，內含 `0 * * * *`（每小時）Cron Trigger，以及更新後的 dist/server/index.js。請沿用專案的 npm run deploy／既有 GitHub 部署流程。部署後在 Cloudflare Worker 的 Triggers / Cron Triggers 確認每小時排程已存在。僅上傳前端檔案不會啟用定時清理。

清理在滿 7 天後的下一次成功排程執行；Cloudflare 執行失敗時順延重試。首次執行會自動建立清理工作表，無須手動清空或重建資料庫。排程使用資料版本檢查，遇到其他人同時還原或儲存時重新讀取，避免用過期資料覆寫。

## 驗證
- 67 項 Node 自動測試通過，含 7 天界線、舊資料寬限、已還原項目保護、關聯資料清除、照片失敗重試。
- 使用測試資料庫與實際 API 的 DOM 流程確認：105 案分頁／搜尋、無零件狀態、隱藏輸入保留、儲存後重新讀取、不重複收料、一般員工操作限制，以及資訊庫搜尋／分頁／匯出筆數。
- 舊版線材裁線、必填照片、權限、批量刪除／還原與登入介面流程回歸通過。
- Worker 已重新建置並檢查語法。本環境未完成真實瀏覽器視覺驗收，也未連接正式資料驗證排程；部署後請確認桌機／手機畫面與 Cloudflare 首次排程紀錄。
