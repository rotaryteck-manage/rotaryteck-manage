# v32 統一數字排序

適用 v31。解壓縮後按原資料夾路徑覆蓋 GitHub，等待 Cloudflare 部署成功，再重新整理網站。

- 前台電鍍、線材格子移除上下左右及拖曳把手，上方改為「調整排序」。
- 後台案件、電鍍、線材各加入「調整排序」，移除原有逐列方向按鈕。
- 後台管理區塊的排序也使用相同數字排序視窗。
- 點按鈕後填寫要排第幾位，其他項目自動順移；按儲存才更新雲端。取消不改動資料。
- 前後台使用同一份順序；保留原本的編號、紀錄、照片及權限。
- 排序視窗列出該區全部項目，不受搜尋框篩選影響。
- 員工、線捆的數字排序沿用；權限按鈕的左右編排維持原設定。

驗證：建置成功、62 項測試通過，另以模擬瀏覽器接後端確認電鍍數字排序、前台方向按鈕移除、各後台排序入口及管理區塊排序保存。尚未部署正式網站。
