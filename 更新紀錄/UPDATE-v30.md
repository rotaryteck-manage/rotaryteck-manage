# v30 線材格子排序

適用 v29，只包含本次變更檔案。解壓後依原路徑覆蓋 GitHub，等 Cloudflare 部署完成，重新整理網站。

- 線材格子加入與電鍍相同的六點拖曳把手，可拖到另一格重新排序。
- 增加上、下按鈕，保留左、右按鈕。上下移動會依目前畫面的欄數交換位置；拖曳則插入目標位置。
- 螢幕縮放或手機橫直切換後，重新計算上下移動位置；無目標位置時按鈕停用。
- 搜尋時只調整搜尋結果內的順序，不改動被隱藏項目的位置。
- 沿用線材修改權限，排序保存到雲端，並留下文字紀錄。
- 後台線材文字設定可修改上移、下移及拖曳排序提示。

驗證：建置成功；模擬瀏覽器接實際後端驗證四欄下移、兩欄上移、拖曳插入、邊界停用、搜尋結果排序與一般員工唯讀，並通過既有線材登記流程檢查。尚未部署正式網站。
