# v45 排版與補貨入口修正

- 案件卡片恢復緊湊兩排，案名、批次與修改按鈕同排；保留搜尋、分頁、預計與實際完成日期、調整排序。
- 案件狀態標題恢復色條，沿用後台提示、資訊、完成配色。
- 庫房隔行底色涵蓋固定首欄與收領料欄，表頭及底部儲存區清楚分隔，沿用後台配色。
- 補貨名單外部按鈕固定開啟目前名單；歷史透過內部按鈕切換。關閉重開仍回目前名單。

## 更新方式
此包以 v44 為基礎。解壓後依原路徑覆蓋 GitHub 中同名檔案，完成既有部署流程後重新整理網站。
不包含資料庫遷移，不更改既有業務資料。

## 驗證
建置成功。瀏覽器實際點擊補貨入口、切換歷史及重開驗證通過；檢查案件卡片欄位排列、庫房整列底色，以及桌面與手機既有介面檢查。UI 操作檢查通過。尚未部署正式網站。
