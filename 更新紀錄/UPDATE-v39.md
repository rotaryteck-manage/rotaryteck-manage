# v39 新增線捆批次輸入

適用已更新至 v38 的網站。依資料夾位置覆蓋檔案，透過原 GitHub／Cloudflare 流程部署。

- 新增線捆預設提供 5 列顏色欄位。
- 按「再增加 5 列」繼續新增欄位，保留已填內容。
- 編號預覽接續該線材目前的線捆數量，無須手動輸入。
- 一次儲存所有已填顏色；空白列略過，儲存後編號自動連續排列，不留下跳號。全部空白時提示填寫。
- 修改既有線捆仍維持單筆編輯。既有排序、紀錄與照片不變。
- 新增按鈕與說明文字可沿用後台線材文字管理設定。

已用 DOM 流程驗證預設 5 列、再加 5 列、輸入保留、空白略過、編號接續、內部編號不重複及全空白阻擋；完成 Worker 建置與語法檢查。尚未部署正式網站。
