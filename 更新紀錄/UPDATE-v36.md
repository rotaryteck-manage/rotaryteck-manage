# v36 庫房專案列對齊修正

接續 v35，修正 v34 統一操作按鈕樣式誤套至庫房整列按鈕，導致專案、點收料、庫房位置、庫存擠在中央的問題。

將整列專案按鈕排除於一般按鈕尺寸規則及自動等寬計算，恢復原本與表頭一致的網格排版，保留原有手機版規則。其他操作按鈕仍統一尺寸。不變更任何資料。

已用模擬瀏覽器檢查專案列 display:grid、表頭與列欄寬一致、一般按鈕與連結按鈕高度一致；完成建置與語法檢查。

解壓縮後依原目錄覆蓋 GitHub 同名檔案，等待 Cloudflare 部署成功，再按 Ctrl+F5 重新整理。尚未部署正式網站。
