# v26 後台分類切換

適用已更新 v25 的專案。解壓後依原路徑覆蓋到 GitHub，等待 Cloudflare 部署成功後重新整理。

- 電鍍案件顯示「案名_電鍍紀錄」，不更改實際案名。先前預設括號尾綴也會改用底線；其他自訂尾綴保留。
- 後台改成上方分類按鈕，下方只顯示選定區域，不再展開收合。
- 網站設定：LOGO、網站共用文字、後台文字。
- 資料備份：匯出全部資料、匯入舊資料、匯出全部照片 ZIP。
- 自訂欄位、頁面與區塊：保留編輯功能與共同草稿的預覽、發布。發布仍會發布完整網站草稿。
- 各管理區原有操作保留。「調整排序」才會顯示上下左右箭頭，每次移動自動儲存；完成後可收起。
- 保留返回庫房按鈕外框、原有共用排序與權限。新分類名稱及排序按鈕文字可於修改後台文字設定。

驗證：51 項自動測試通過；另以模擬瀏覽器確認單區顯示、九個分類、按鈕分組、預覽發布切換、排序儲存、重新繪製後保留分類，以及電鍍案名尾綴。未代為部署正式網站。
