# v25 介面與全部照片匯出

適用已更新 v24 的專案。解壓後依原資料夾位置覆蓋到 GitHub，等待 Cloudflare 部署完成，再重新整理網站。

- 電鍍案件按鈕顯示「案名（電鍍紀錄）」，實際案名不改。尾綴可在電鍍管理的修改本區文字調整。
- 後台桌面改為兩欄，手機一欄。每個區塊有上下左右箭頭，移動會自動儲存，所有主管共用相同順序。
- 管理後台上方新增「匯出全部照片 ZIP」，只有主管能使用此後台功能。原本庫房及電鍍分區匯出權限不變。
- ZIP 依庫房／電鍍、案件、送鍍次數、出貨單／表面積分類，包含庫房保留中的已刪除案件照片、網站 LOGO、照片總表與說明。案件資料夾加序號，避免同名覆蓋。
- 照片總表包含案名、種類、人員、上傳時間、原始名稱及檔案位置。文字資料仍請用「匯出全部資料」備份。
- 任一照片下載失敗不會產生不完整 ZIP。單次照片總量超過 250 MiB 時提示改用分案匯出，以避免瀏覽器記憶體不足。大量照片建議用電腦匯出。
- 返回庫房連結加上按鈕外框。

驗證：51 項測試通過，另完成模擬瀏覽器後台排序、重繪保留順序及原有電鍍操作流程。尚未代為部署正式網站。
